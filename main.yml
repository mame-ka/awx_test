---

- name: install httpd server
  hosts: redhat
  become: yes
  tasks:
    - name: install tomcat
      yum:
        name: tomcat
        state: present
    - name: start tomcat service
      service:
        name: tomcat
        state: started
        enabled: yes
